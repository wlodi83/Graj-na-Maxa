<%= form_for(@age_bracket) do |f| %>
  <% if @age_bracket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@age_bracket.errors.count, "error") %> prohibited this age_bracket from being saved:</h2>

      <ul>
      <% @age_bracket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
